SELECT NAME
     , COUNT(NAME) AS COUNT
FROM ANIMAL_INS
GROUP BY NAME
HAVING COUNT >=2
ORDER BY NAME










# -- 코드를 입력하세요
# select NAME,
# count(ANIMAL_ID) as COUNT
# FROM ANIMAL_INS
# GROUP BY NAME
# HAVING count(NAME) >= 2
# order by NAME asc